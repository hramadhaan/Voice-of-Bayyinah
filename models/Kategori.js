class Kategori {
  constructor(id, kategori, gambar, fileName) {
    this.id = id;
    this.kategori = kategori;
    this.gambar = gambar;
    this.fileName = fileName;
  }
}

export default Kategori;
